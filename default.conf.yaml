- clean:
    ~/:
        recursive: False
    ~/.config:
        recursive: True
    ~/.local:
        recursive: True
- link:
    ~/.tmux.conf: tmux/tmux.conf
    ~/.vimrc: vim/vimrc
    ~/.gitconfig: git/gitconfig
    ~/.config/fish/config.fish: env/config.fish
- shell: []
